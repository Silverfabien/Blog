vich_uploader:
    db_driver: orm

    mappings:
        article_pictures:
            uri_prefix: /uploads/pictures/articles
            upload_destination: '%kernel.project_dir%/public/uploads/pictures/articles'
            namer: vich_uploader.namer_uniqid
